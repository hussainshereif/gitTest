import { AppUsageDatePipe } from './app-usage-date.pipe';

describe('AppUsageDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AppUsageDatePipe();
    expect(pipe).toBeTruthy();
  });
});
